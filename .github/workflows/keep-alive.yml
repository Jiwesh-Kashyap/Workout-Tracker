name: Keep Render App Awake

on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render URL
        run: curl -s https://workout-tracker-y064.onrender.com > /dev/null
